version: '1'

setup:
  abort_on_failure: true
  image: us.icr.io/genctl-cicd-onepipeline/genctl/golang-ci:22.20260817071533
  script: |
    #!/usr/bin/env bash

    export PATH_TO_GENCTL_CI="${WORKSPACE}/one-pipeline-config-repo"
    export PATH_TO_PIPELINE=${PATH_TO_GENCTL_CI}/onepipeline/pipelines/cd/templatized/promotion/pr_master
    ${PATH_TO_PIPELINE}/steps/setup.sh
test:
  abort_on_failure: false
  dind: true
  image: us.icr.io/genctl-cicd-onepipeline/genctl/golang-ci:22.20260817071533
  script: |
    #!/usr/bin/env bash

    export PATH_TO_GENCTL_CI="${WORKSPACE}/one-pipeline-config-repo"
    export PATH_TO_PIPELINE=${PATH_TO_GENCTL_CI}/onepipeline/pipelines/cd/templatized/promotion/pr_master
    ${PATH_TO_PIPELINE}/steps/test.sh
promotion-tests-as-subpipeline:
  image: us.icr.io/genctl-cicd-onepipeline/genctl/golang-ci:22.20260817071533
  dind: true
  abort_on_failure: true
  image_pull_policy: IfNotPresent
  script: |
    #!/usr/bin/env bash
    export PATH_TO_GENCTL_CI="${WORKSPACE}/one-pipeline-config-repo"
    export PATH_TO_PIPELINE=${PATH_TO_GENCTL_CI}/onepipeline/pipelines/cd/templatized/promotion/pr_master
    ${PATH_TO_PIPELINE}/steps/promotion_tests_as_subpipeline.sh
qz2-disconnect-cos-remote-resource:
  abort_on_failure: true
  image_pull_policy: IfNotPresent
  dind: true
  image: us.icr.io/genctl-cicd-onepipeline/genctl/golang-ci:22.20260320065041
  script: |
    #!/usr/bin/env bash
    export PATH_TO_GENCTL_CI="${WORKSPACE}/one-pipeline-config-repo"
    export PATH_TO_PIPELINE=${PATH_TO_GENCTL_CI}/onepipeline/pipelines/cd/templatized/promotion/pr_master
    ${PATH_TO_PIPELINE}/steps/qz2-disconnect-cos-remote-resource.sh
qz2-cluster-validations:
  abort_on_failure: true
  image_pull_policy: IfNotPresent
  dind: true
  image: us.icr.io/genctl-cicd-onepipeline/genctl/golang-ci:22.20260320065041
  script: |
    #!/usr/bin/env bash
    export PATH_TO_GENCTL_CI="${WORKSPACE}/one-pipeline-config-repo"
    export PATH_TO_PIPELINE=${PATH_TO_GENCTL_CI}/onepipeline/pipelines/cd/templatized/promotion/pr_master
    ${PATH_TO_PIPELINE}/steps/qz2-cluster-validation.sh
qz2-reconnect-cos-remote-resource:
  abort_on_failure: true
  image_pull_policy: IfNotPresent
  dind: true
  image: us.icr.io/genctl-cicd-onepipeline/genctl/golang-ci:22.20260320065041
  script: |
    #!/usr/bin/env bash
    export PATH_TO_GENCTL_CI="${WORKSPACE}/one-pipeline-config-repo"
    export PATH_TO_PIPELINE=${PATH_TO_GENCTL_CI}/onepipeline/pipelines/cd/templatized/promotion/pr_master
    ${PATH_TO_PIPELINE}/steps/qz2-reconnect-cos-remote-resource.sh
code-pr-finish:
  abort_on_failure: true
  dind: true
  image: us.icr.io/genctl-cicd-onepipeline/genctl/golang-ci:22.20260817071533
  script: |
    #!/usr/bin/env bash

    export PATH_TO_GENCTL_CI="${WORKSPACE}/one-pipeline-config-repo"
    export PATH_TO_PIPELINE=${PATH_TO_GENCTL_CI}/onepipeline/pipelines/cd/templatized/promotion/pr_master
    ${PATH_TO_PIPELINE}/steps/unlock.sh
